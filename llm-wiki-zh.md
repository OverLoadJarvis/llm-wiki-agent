# LLM 维基

使用 LLM 构建个人知识库的一种模式。

这是一个创意文件，旨在复制粘贴到您自己的 LLM 代理中（例如 OpenAI Codex、Claude Code、OpenCode / Pi 等）。它的目标是传达高层级的想法，但您的代理将与您协作构建具体细节。

## 核心思想

大多数人使用 LLM 和文档的体验类似于 RAG：您上传一组文件，LLM 在查询时检索相关片段，然后生成答案。这确实有效，但 LLM 在每个问题上都在从零开始重新发现知识。没有积累。如果您提出一个需要综合五份文档的微妙问题，LLM 每次都必须找到并拼凑相关片段。没有建立任何东西。NotebookLM、ChatGPT 文件上传以及大多数 RAG 系统都是这样工作的。

这里的想法不同。与其仅在查询时从原始文档中检索，不如让 LLM **增量构建并维护一个持久化的维基**——一个结构化、相互链接的 Markdown 文件集合，位于您和原始来源之间。当您添加新来源时，LLM 不仅仅是为以后的检索建立索引。它会阅读它，提取关键信息，并将其集成到现有维基中——更新实体页面、修订主题摘要、标记新数据与旧主张相矛盾的地方、加强或挑战不断发展的综合。知识被编译一次，然后*保持最新*，而不是在每次查询时重新推导。

这是关键区别：**维基是一个持久、复合的产物。** 交叉引用已经存在。矛盾已经被标记。综合已经反映了您所阅读的一切。每添加一个来源、每提出一个问题，维基都会变得更加丰富。

您从不（或很少）自己编写维基——LLM 编写并维护所有内容。您负责来源、探索和提出正确的问题。LLM 完成所有繁重的工作——总结、交叉引用、归档和簿记，这些工作使知识库随着时间的推移真正有用。在实践中，我会让 LLM 代理在一侧打开，Obsidian 在另一侧打开。LLM 根据我们的对话进行编辑，我实时浏览结果——跟随链接、检查图形视图、阅读更新的页面。Obsidian 是 IDE；LLM 是程序员；维基是代码库。

这可以应用于许多不同的上下文。几个例子：

- **个人**：跟踪您自己的目标、健康、心理、自我提升——归档日记条目、文章、播客笔记，并随着时间的推移构建关于您自己的结构化图景。
- **研究**：在数周或数月内深入研究一个主题——阅读论文、文章、报告，并增量构建一个全面的维基，包含不断发展的论文。
- **阅读一本书**：随着阅读进度归档每一章，为角色、主题、情节线索以及它们的联系方式构建页面。到最后您会有一个丰富的配套维基。想想像 [Tolkien Gateway](https://tolkiengateway.net/wiki/Main_Page) 这样的粉丝维基——数千个相互链接的页面涵盖角色、地点、事件、语言，由志愿者社区多年来构建。您可以在阅读时亲自构建类似的东西，由 LLM 完成所有交叉引用和维护。
- **业务/团队**：由 LLM 维护的内部维基，由 Slack 线程、会议记录、项目文档、客户电话提供信息。可能有人工参与审查更新。维基保持最新，因为 LLM 完成了团队中没人想做的维护工作。
- **竞争分析、尽职调查、旅行计划、课程笔记、爱好深入研究**——任何您随着时间积累知识并希望其有序而非分散的事情。

## 架构

有三层：

**原始来源**——您精选的源文档集合。文章、论文、图片、数据文件。这些是不可变的——LLM 从中读取但从不修改它们。这是您的真相来源。

**维基**——LLM 生成的 Markdown 文件目录。摘要、实体页面、概念页面、比较、概述、综合。LLM 完全拥有这一层。它创建页面，在新来源到达时更新它们，维护交叉引用，并保持所有内容一致。您阅读它；LLM 编写它。

**架构**——一个文档（例如 Claude Code 的 CLAUDE.md 或 Codex 的 AGENTS.md），告诉 LLM 维基的结构、约定是什么，以及在摄入来源、回答问题或维护维基时遵循什么工作流程。这是关键的配置文件——它使 LLM 成为一个训练有素的维基维护者，而不是通用的聊天机器人。当您弄清楚什么适合您的领域时，您和 LLM 会随着时间共同演进这个文件。

## 操作

**摄入。** 您将一个新来源放入原始集合中，并告诉 LLM 处理它。一个示例流程：LLM 阅读来源，与您讨论关键要点，在维基中编写摘要页面，更新索引，更新维基中相关的实体和概念页面，并在日志中附加一个条目。一个单一的来源可能会触及 10-15 个维基页面。就我个人而言，我更喜欢一次一个地摄入来源并保持参与——我阅读摘要，检查更新，并指导 LLM 强调什么。但您也可以在较少监督的情况下批量摄入许多来源。由您来开发适合您风格的工作流程，并将其记录在架构中以备将来使用。

**查询。** 您向维基提出问题。LLM 搜索相关页面，阅读它们，并综合一个带有引用的答案。根据问题，答案可以采取不同的形式——Markdown 页面、比较表、幻灯片（Marp）、图表（matplotlib）、画布。重要的洞察：**好的答案可以作为新页面归档回维基。** 您要求的比较、分析、您发现的联系——这些都是有价值的，不应消失在聊天历史中。这样，您的探索就像摄入的来源一样在知识库中复合。

**梳理。** 定期要求 LLM 对维基进行健康检查。寻找：页面之间的矛盾、被新来源取代的过时主张、没有入站链接的孤立页面、被提及但缺少自己页面的重要概念、缺失的交叉引用、可以通过网络搜索填补的数据空白。LLM 擅长建议需要调查的新问题和需要寻找的新来源。随着维基的增长，这可以保持其健康。

## 索引和日志

两个特殊文件帮助 LLM（以及您）在维基增长时导航它。它们服务于不同的目的：

**index.md** 是面向内容的。它是维基中所有内容的目录——每个页面都列出了链接、一行摘要，以及可选的元数据，如日期或来源数量。按类别（实体、概念、来源等）组织。LLM 在每次摄入时更新它。回答查询时，LLM 首先阅读索引以找到相关页面，然后深入研究它们。这在中等规模下（~100 个来源，~数百个页面）工作得非常好，并且避免了基于嵌入的 RAG 基础设施的需要。

**log.md** 是按时间顺序的。它是发生的事情和时间的仅追加记录——摄入、查询、梳理。一个有用的提示：如果每个条目都以一致的前缀开头（例如 `## [2026-04-02] ingest | Article Title`），日志就可以用简单的 Unix 工具解析——`grep "^## \[" log.md | tail -5` 给您最后 5 个条目。日志为您提供维基演进的时间线，并帮助 LLM 了解最近完成了什么。

## 可选：CLI 工具

在某个时候，您可能希望构建小型工具来帮助 LLM 更高效地操作维基。维基页面的搜索引擎是最明显的一个——在小规模下索引文件就足够了，但随着维基的增长，您需要适当的搜索。[qmd](https://github.com/tobi/qmd) 是一个不错的选择：它是 Markdown 文件的本地搜索引擎，具有混合 BM25/向量搜索和 LLM 重新排序，全部在设备上。它同时具有 CLI（因此 LLM 可以调用它）和 MCP 服务器（因此 LLM 可以将其用作原生工具）。您也可以自己构建更简单的东西——当需要时，LLM 可以帮助您 vibe-code 一个简单的搜索脚本。

## 提示和技巧

- **Obsidian Web Clipper** 是一个浏览器扩展，可将网页文章转换为 Markdown。对于快速将来源放入您的原始集合非常有用。
- **本地下载图像。** 在 Obsidian 设置 → 文件和链接中，将“附件文件夹路径”设置为固定目录（例如 `raw/assets/`）。然后在设置 → 热键中，搜索“下载”找到“为当前文件下载附件”并将其绑定到热键（例如 Ctrl+Shift+D）。剪切文章后，按下热键，所有图像都会下载到本地磁盘。这是可选的，但很有用——它让 LLM 直接查看和引用图像，而不是依赖可能会失效的 URL。请注意，LLM 无法一次性本地读取带有内联图像的 Markdown——解决方法是让 LLM 先阅读文本，然后单独查看部分或全部引用的图像以获取额外上下文。这有点笨拙，但效果足够好。
- **Obsidian 的图形视图** 是查看维基形状的最佳方式——什么与什么连接，哪些页面是枢纽，哪些是孤立的。
- **Marp** 是一种基于 Markdown 的幻灯片格式。Obsidian 有它的插件。用于直接从维基内容生成演示文稿非常有用。
- **Dataview** 是一个 Obsidian 插件，可对页面前置元数据运行查询。如果您的 LLM 向维基页面添加 YAML 前置元数据（标签、日期、来源数量），Dataview 可以生成动态表格和列表。
- 维基只是一个 Markdown 文件的 git 仓库。您免费获得版本历史、分支和协作。

## 为什么这有效

维护知识库的繁琐部分不是阅读或思考——而是簿记。更新交叉引用、保持摘要最新、标记新数据与旧主张相矛盾的地方、保持数十个页面之间的一致性。人类放弃维基是因为维护负担的增长速度快于价值。LLM 不会感到无聊，不会忘记更新交叉引用，并且可以在一次操作中触及 15 个文件。维基保持维护，因为维护成本几乎为零。

人类的工作是精选来源、指导分析、提出好问题，并思考这一切意味着什么。LLM 的工作是其他一切。

这个想法在精神上与 Vannevar Bush 的 Memex（1945）有关——一个个人、精选的知识存储，在文档之间具有关联路径。Bush 的愿景比网络变成的样子更接近这个：私人、主动精选，文档之间的联系与文档本身一样有价值。他无法解决的部分是谁来做维护。LLM 处理了这一点。


## 注意

本文档有意保持抽象。它描述了想法，而不是具体的实现。确切的目录结构、架构约定、页面格式、工具——所有这些都将取决于您的领域、您的偏好以及您选择的 LLM。上面提到的所有内容都是可选和模块化的——选择有用的，忽略无用的。例如：您的来源可能只是纯文本，所以您根本不需要图像处理。您的维基可能足够小，索引文件就是您需要的，不需要搜索引擎。您可能不关心幻灯片，只想要 Markdown 页面。您可能想要一套完全不同的输出格式。使用这个的正确方法是与您的 LLM 代理共享它，并一起实例化一个适合您需求的版本。本文档的唯一工作是传达模式。您的 LLM 可以弄清楚其余部分。
